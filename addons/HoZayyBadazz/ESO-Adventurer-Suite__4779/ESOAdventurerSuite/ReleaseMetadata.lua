-- ESO Adventurer Suite
-- Canonical release identity loaded immediately after Core.lua so diagnostics,
-- self-tests, and public release metadata stay synchronized.
local EPC = ESOProgressionCoach
if not EPC then return end

EPC.version = "0.29.696"
EPC.addOnVersion = 3631
