# Satuve Xbox UI 1.9.13

- Tracks ownership of Game Camera UI mode while the Combat Log is open.
- Releases Camera UI mode after the existing V1 focus and keybind-strip cleanup, but only when the report enabled it.
- Uses the base-scene transition so the ESO gamepad menu is no longer visible behind the Combat Log.
- Does not change the restored V1 controller navigation or bindings.
