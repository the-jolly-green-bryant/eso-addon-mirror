# Satuve Xbox UI 1.9.26

- Removes the remaining temporary Combat Log camera-state chat logging and its diagnostic-only historical release artifact.
- Preserves the working Combat Log controller input, focus acquisition/release, hierarchical Back behavior, mouse support, camera-mode ownership and native keybind hints.
- Preserves the final RaidFrame canonical `TOPLEFT`/`TOPLEFT` save/restore behavior and legacy-anchor compatibility.
- Makes no functional changes to other UI systems.
