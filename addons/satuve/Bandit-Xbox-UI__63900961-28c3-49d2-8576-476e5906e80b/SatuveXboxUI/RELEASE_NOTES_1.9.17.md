# Satuve Xbox UI 1.9.17

- Fixes the Group Frame returning to the wrong position after `/reloadui`.
- Correctly converts the Frame Editor's `CENTER/CENTER` position to the Group Frame's stored `TOPLEFT/CENTER` position.
- Does not change Player Frame, Target Frame, other movable frames, Combat Log, Statistics, sidebar, Custom Bar, Quick Menu or controller navigation.
